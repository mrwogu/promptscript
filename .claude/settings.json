{
  "plansDirectory": ".claude/plans",
  "env": {
    "DISABLE_TELEMETRY": "1",
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "enabledPlugins": {
    "code-simplifier@claude-plugins-official": true,
    "feature-dev@claude-plugins-official": true,
    "frontend-design@claude-plugins-official": true,
    "ralph-loop@claude-plugins-official": true,
    "security-guidance@claude-plugins-official": true,
    "typescript-lsp@claude-plugins-official": true,
    "superpowers@claude-plugins-official": true,
    "caveman@caveman": true,
    "remember@claude-plugins-official": true
  },
  "extraKnownMarketplaces": {
    "caveman": {
      "source": {
        "source": "github",
        "repo": "JuliusBrussee/caveman"
      }
    }
  },
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); model=$(echo \"$input\" | jq -r '.model.display_name'); used=$(echo \"$input\" | jq -r '.context_window.used_percentage // empty'); max_tokens=$(echo \"$input\" | jq -r '.context_window.context_window_size // 0'); branch=$(git -C $(pwd) branch --show-current 2>/dev/null || echo 'no-git'); if [ -n \"$used\" ]; then used_k=$(echo \"$used $max_tokens\" | awk '{printf \"%.0f\", $1 * $2 / 100000}'); max_k=$((max_tokens / 1000)); printf \"%s | Context: %.1f%% [%dk/%dk] | %s\" \"$model\" \"$used\" \"$used_k\" \"$max_k\" \"$branch\"; else printf \"%s | %s\" \"$model\" \"$branch\"; fi"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "prs hook pre-edit",
            "timeout": 5,
            "statusMessage": "PromptScript: checking generated files..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "prs hook post-edit",
            "timeout": 15,
            "statusMessage": "PromptScript: compiling..."
          }
        ]
      },
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -z \"${CLAUDE_PROJECT_DIR:-}\" ]; then printf '%s\\n' 'PromptScript claude hook requires non-empty CLAUDE_PROJECT_DIR.' >&2; exit 1; fi; cd \"${CLAUDE_PROJECT_DIR}\" && sh \"${CLAUDE_PROJECT_DIR}\"/.promptscript/scripts/graph-update.sh # promptscript-generated:graph-update",
            "timeout": 30,
            "statusMessage": "code-review-graph: refreshing index..."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -z \"${CLAUDE_PROJECT_DIR:-}\" ]; then printf '%s\\n' 'PromptScript claude hook requires non-empty CLAUDE_PROJECT_DIR.' >&2; exit 1; fi; cd \"${CLAUDE_PROJECT_DIR}\" && sh \"${CLAUDE_PROJECT_DIR}\"/.promptscript/scripts/graph-status.sh # promptscript-generated:graph-status",
            "timeout": 10,
            "statusMessage": "code-review-graph: reading index status..."
          }
        ]
      }
    ]
  }
}
